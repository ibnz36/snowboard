[package]
name = "json"
version = "0.1.0"
edition = "2024"

[dependencies.snowboard]
path = "../../"
features = ["json"]

[dependencies]
serde_json = "*"
serde = { version = "*", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
