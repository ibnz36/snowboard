[package]
name = "perf-test"
version = "0.1.0"
edition = "2021"

[dependencies.snowboard]
path = "../../"
features = []

[dependencies]
smol-potat = {version ="*", features = ["auto"]}
mimalloc = { version = "0.1", default-features = false }

[profile.release]
debug = true
